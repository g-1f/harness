# Shared operations

This reference is consolidated into [runtime.md](runtime.md). It contains the
current primitive inventory, sharing and retry identity, lease dependencies,
cancellation behavior, wrapper contract, limits and verification pointers.
